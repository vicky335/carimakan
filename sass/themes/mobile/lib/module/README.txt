對於一些小組件，都放在了modules/文件夾（通常也稱為components/），layout/是一個宏觀的（定義全局的線框），components/是一個微觀的。它裡面放了一些特定的組件，比如說slider，loading，widget或者其他的小組件。通常components/目錄下的都是一些小組件文件。

_media.scss
_carousel.scss
_thumbnails.scss
